[libdefaults]
 default_realm = {{ ad_realm }}
 dns_lookup_realm = false
 dns_lookup_kdc = true

[realms]
 {{ ad_realm }} = {
  kdc = {{ ad_server }}
  admin_server = {{ ad_server }}
 }

[domain_realm]
 .{{ ad_domain }} = {{ ad_realm }}
 {{ ad_domain }} = {{ ad_realm }}
